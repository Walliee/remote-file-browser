If the "testcase" directory is made to be the toplevel directory, this directory named "static" could be shadowed
by the server's own "static/" content directory.

However, this directory will be accessible when viewing it under the "browse/" path.
